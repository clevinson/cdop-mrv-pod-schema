[project]
name = "cdop-mrv-pod-schemas"
version = "0.1.0"
description = "CDOP MRV pod schema proof of concept"
requires-python = ">=3.11"
dependencies = [
    "linkml>=1.11.1,<2",
    "mkdocs-material>=9.6,<10",
]

[tool.uv]
package = false
